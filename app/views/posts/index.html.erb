<%= render "shared/header" %>
<div class='main'>
  <div class="recipe-new">新着のレシピ</div>
  <div class="recipe-grid">
    <% @posts.each do |post| %>
      <div class="recipe-card">
        <%= post.title %>
        <%= image_tag post.image.variant(resize: "260x260!"), class: 'post-image' if post.image.attached? %>
      </div>
    <% end %>
  </div>
</div>
<%= link_to new_post_path, class: 'purchase-btn' do %>
  <span class='purchase-btn-text'>投稿する</span>
  <%= image_tag 'coooking.png', class: "purchase-btn-icon" %>
<% end %>